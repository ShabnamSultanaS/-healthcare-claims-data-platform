# -healthcare-claims-data-platform
End-to-end ELT data platform for healthcare claims — medallion architecture, data quality framework, star schema (Python, SQL, DuckDB)
cd path/to/healthcare-claims-data-platform

git init
git add .
git commit -m "Healthcare claims data platform: medallion ELT pipeline with DQ framework"
git branch -M main
git remote add origin https://github.com/ShabnamSultanaS/healthcare-claims-data-platform.git
git push -u origin main
